pub const Pos2D = struct {
    x: i32 = 0,
    y: i32 = 0,
};

pub const Size2D = struct {
    width: u32 = 0,
    height: u32 = 0,
};

pub const Pos3D = struct {
    x: f32 = 0,
    y: f32 = 0,
    z: f32 = 0,
};

pub const Size3D = struct {
    width: f32 = 0,
    height: f32 = 0,
    depth: f32 = 0,
};
